---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<header class="page-heading fade-in">
  <h1>{title}</h1>
  {subtitle && <p>{subtitle}</p>}
</header>
